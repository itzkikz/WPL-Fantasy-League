// src/routes/books/$id.tsx
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/standings/$id')({
  component: BookDetail,
})

function BookDetail() {
  const { id } = Route.useParams()
  return <h1>Book ID: {id}</h1>
}
